# Example output

The HTML and JSON reports in this folder were generated from `sample_data/demo_events.json` with identifier pseudonymisation enabled:

```bash
triagebloom analyze sample_data/demo_events.json --output-dir examples --redact
```

The source dataset is synthetic. The example demonstrates report structure and should not be interpreted as a production validation result.
